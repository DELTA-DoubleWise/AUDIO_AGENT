[project]
name = "evaluation-tool"
version = "1.0.0"
description = "Tool evaluation and RPS-based tool selection for AUDIO_AGENT"
requires-python = ">=3.11"
authors = [
    {name = "Audio Agent Team"}
]

dependencies = [
    # Uses audio_agent.evaluation from parent package
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
